<li >
  <% if link.is_dir %>
    <%= link_to raw("<i class='icon-folder-close'></i>#{scoped_path link.path}"), "/home/#{link.path}" %>
  <% else %>
    <%= link_to raw("<i class='icon-file'></i>#{scoped_path link.path}"), "/files/#{link.path}" %>
  <% end %>
<li>