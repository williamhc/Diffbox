<% if not user_signed_in? %>
  <div class="hero-unit">
    <h1>Diffbox:<br>instant version control</h1>
    <p>Diffbox is a free service that allows you to view your files in your Dropbox account, compare them against previous versions, and even revert your file if you wish.</p>
    <p>
      <%= link_to "Sign up now", new_user_registration_path, :class => "btn btn-primary btn-large" -%>
    </p>
  </div>  
<% elsif not current_user.authorized_with_dropbox? %>
  <div class="hero-unit">
    <h1>One more step...</h1>
    <p>In order to access your Dropbox files we need you to authorize the Diffbox app with your Dropbox account. You will be redirected back to Diffbox after authorizing. And you'll be ready to <em>get diff'n!</em></p>
    <p>
      <%= link_to "Authorize Diffbox", { :controller => "dropbox_auth", :action => "authorize"} , :class => "btn btn-primary btn-large" -%>
    </p>
  </div>
<% else %>
  <%= render :partial => "breadcrumbs" %>
</div>
<div class="row-fluid">
  <div class="span2">
    <ul class="nav nav-pills nav-stacked">
      <%= render :partial => "nav_link", :collection => @links, :as => :link %>
    </ul>
  </div>
  <div class="span4 well">
    <% @file || "this is the first document" %>
  </div>
  <div class="span4 well">
    this is the 2nd document
  </div>
  <div class="span2 well">
    here are all the previous versions...
  </div>
<% end %>
